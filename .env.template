# Flask
FLASK_APP=run.py
FLASK_ENV=development
SECRET_KEY=DEV-ONLY-CHANGE-ME

# SQLite lives in /app/data (bind mount to ./data)
SQLALCHEMY_DATABASE_URI=sqlite:////app/data/infratrack.db

# Gunicorn bind (compose maps 5000 -> nginx)
HOST=0.0.0.0
PORT=5000